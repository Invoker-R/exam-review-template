# 考试点评模板

这是一个可直接托管的静态网页模板，不依赖后端服务。

## 文件

- `index.html`：页面入口
- `styles.css`：界面样式
- `app.js`：选择逻辑、话术数据、综合评语生成

## 使用

直接打开 `index.html` 即可使用。部署时把这四个文件放到任意静态服务器、Nginx、GitHub Pages、Netlify、Vercel 或学校服务器目录中。

## 逻辑

- 图片描述：A 同学、B 同学各选择一次
- 关系链接：选择一次
- 逻辑推进：选择一次
- 主题提炼：选择一次
- 可随时生成综合评语，未选择的模块会自动跳过
